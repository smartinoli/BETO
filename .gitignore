
# cuotas históricas de OddsPapi: una respuesta por partido, crece rápido
# y se puede volver a bajar. El resumen (itf-cuotas-historicas.json) sí se
# versiona, que es lo que se analiza.
vigia/datos/itf/historico/

# playwright instalado a mano para rescatar cuadros desde la sesion; no va al repo
node_modules/
